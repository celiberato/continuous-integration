SONAR QUBE:



https://www.sonarqube.org/

docker run -d --name sonarqube -e SONAR_ES_BOOTSTRAP_CHECKS_DISABLE=true -p 9000:9000 sonarqube:latest

localhost:9000

admin/123456

docker pull sonarsource/sonar-scanner-cli

export SONARQUBE_URL=localhost:9000
export YOUR_PROJECT_KEY=go-project
export YOUR_REPO=repo

docker run \
    --rm \
    -e SONAR_HOST_URL="http://${SONARQUBE_URL}" \
    -e SONAR_SCANNER_OPTS="-Dsonar.projectKey=${YOUR_PROJECT_KEY}"
    -e SONAR_LOGIN="myAuthenticationToken" \
    -v "${YOUR_REPO}:/usr/src" \
    sonarsource/sonar-scanner-cli

sonar-scanner \
  -Dsonar.projectKey=go-project \
  -Dsonar.sources=. \
  -Dsonar.host.url=http://localhost:9000 \
  -Dsonar.login=sqp_5ab420094020820404a7367c9e598ae882d569ca




  docker run --rm -e SONAR_HOST_URL="http://${SONARQUBE_URL}" -e SONAR_SCANNER_OPTS="-Dsonar.projectKey=${YOUR_PROJECT_KEY}" -e SONAR_LOGIN="myAuthenticationToken"  -v "${YOUR_REPO}:/usr/src" sonarsource/sonar-scanner-cli